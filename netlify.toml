[build]
  command = "NODE_ENV=production pnpm run db:clear-dev-migration && NODE_ENV=production pnpm run db:repair-migration-state && NODE_ENV=production pnpm run payload migrate && NODE_ENV=production pnpm run db:verify-schema && NODE_ENV=production pnpm run build"
  publish = ".next"
